# Web Framework & API
fastapi==0.109.0
uvicorn==0.27.0
sqlalchemy>=2.0.36

# Auth (JWT + form parsing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP Client (legacy desktop client)
requests==2.31.0

# Database
# sqlite3 is built into Python, so it is not listed here.
